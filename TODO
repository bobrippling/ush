nohup + print background tasks on exit

readline: tab completion
parse:    $varname, *, etc

history

cd, fg, rehash

parsing: `echo hi | gr<TAB>` should complete to grep

copy zsh - open stdin as terminal, instead of stdout?
(poormanseperation.sh)

restructure to be more focused on the syntax tree/parser

There are running jobs.  Why don't you go chase them?

ush -c ncurses-app
fails - SIGTSTP
